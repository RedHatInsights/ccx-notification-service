name: Go tests

on:
  push:
    branches: ["master"]
  pull_request:

permissions:
  contents: read

jobs:
  gotests:
    uses: RedHatInsights/processing-tools/.github/workflows/gotests.yaml@master
    with:
      coverage_threshold: 0
    secrets: inherit
